import { CheckCircle2, Construction, ShieldCheck } from 'lucide-react'
import { useAuth } from '../hooks/useAuth'

export function WorkspacePage() {
  const { user } = useAuth()
  return <main className="p-5 md:p-8"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-cyan-400">Authenticated workspace</p><h1 className="mt-2 text-2xl font-semibold tracking-tight text-white md:text-3xl">Welcome, {user?.username}</h1><p className="mt-2 text-sm text-slate-500">The secure application foundation is connected and ready.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-full border border-emerald-400/20 bg-emerald-400/5 px-3 py-1.5 text-xs font-semibold text-emerald-300"><CheckCircle2 className="size-3.5" />Session verified</span></div><section className="mt-8 grid gap-4 md:grid-cols-3"><div className="surface-card p-5"><ShieldCheck className="size-5 text-cyan-400" /><h2 className="mt-5 text-sm font-semibold text-slate-100">Identity established</h2><p className="mt-2 text-xs leading-5 text-slate-500">Profile verified through the ThreatLyst `/api/auth/me` endpoint.</p></div><div className="surface-card p-5"><div className="text-xs font-bold uppercase tracking-[0.16em] text-slate-600">Current role</div><p className="mt-5 text-2xl font-semibold capitalize text-white">{user?.role}</p><p className="mt-2 text-xs leading-5 text-slate-500">Navigation and actions use centralized RBAC helpers.</p></div><div className="surface-card border-dashed p-5"><Construction className="size-5 text-amber-400" /><h2 className="mt-5 text-sm font-semibold text-slate-100">Operational modules</h2><p className="mt-2 text-xs leading-5 text-slate-500">Dashboard and investigation surfaces are intentionally deferred to later phases.</p></div></section></div></main>
}
